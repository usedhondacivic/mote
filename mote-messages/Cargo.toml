[package]
name = "mote-messages"
version = "0.0.0"
edition = "2024"

[dependencies]
defmt = "1.0.1"
heapless = { version = "0.8", features = ["serde", "defmt-03"] }
serde = { version = "1.0.*", default-features = false, features = ["derive"] }
