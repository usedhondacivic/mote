[default]
_default:
    just --list

test:
    cargo test

build:
    cargo build --release

deploy:
    cargo run --release

format:
    cargo fmt

lint:
    @echo "Linting mote-api"
    cargo clippy --all-features

format-check:
    cargo fmt --check

ci: build format-check
